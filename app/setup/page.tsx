"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function Setup(){const r=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const res=await fetch("/api/auth/setup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await res.json();setBusy(false);if(!res.ok)return setError(d.error||"Setup failed");r.replace(d.redirect);r.refresh()}return <main className="login-screen"><div className="login-panel" style={{margin:"auto"}}><form className="login-card" onSubmit={submit}><div className="login-mobile-brand"><div className="brand-mark">D</div><strong>DMS First Setup</strong></div><h2>Create administrator</h2><p>This page works only before the first DMS user is created.</p><label className="login-field"><span>Name</span><input name="displayName" required placeholder="Administrator name"/></label><label className="login-field"><span>Username</span><input name="username" required placeholder="admin"/></label><label className="login-field"><span>Password</span><input name="password" type="password" minLength={6} required placeholder="Minimum 6 characters"/></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary login-btn" disabled={busy}>{busy?"Creating…":"Create Admin"}</button></form></div></main>}
