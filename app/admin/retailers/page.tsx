import {requireUser} from "../../../lib/auth";import {normalizeMonth} from "../../../lib/drilldown";import {retailerOpportunities} from "../../../lib/retailer-opportunities";import {PageHead} from "../../components/RoleUI";import {RetailerSearchView} from "../../components/RetailerOpportunityViews";
export default async function Page({searchParams}:{searchParams:Promise<{q?:string;month?:string}>}){await requireUser(["ADMIN"]);const s=await searchParams,month=normalizeMonth(s.month),rows=await retailerOpportunities(month);return <main className="page"><PageHead eyebrow="Admin" title="Retailer search" subtitle="Search the full retailer base and open live monthly performance details."/><RetailerSearchView rows={rows} month={month} q={s.q||""} base="/admin/retailers"/></main>}
