import {requireUser} from "../../../../../lib/auth";
import {prisma} from "../../../../../lib/prisma";
import AdminEmployeeForm from "../../../../components/AdminEmployeeForm";
import ManagerTeamEditor from "../../../../components/ManagerTeamEditor";
import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{id:string}>}){await requireUser(["ADMIN"]);const {id}=await params,[u,supervisors]=await Promise.all([prisma.user.findUnique({where:{id},include:{managedSupervisors:true}}),prisma.supervisor.findMany({orderBy:{name:"asc"},include:{managerAssignments:{include:{manager:{select:{displayName:true}}}},_count:{select:{employees:true}}}})]);if(!u||u.role!=="MANAGER")notFound();return <><AdminEmployeeForm role="managers" initial={{id:u.id,name:u.displayName,mobile:u.mobileNumber||"",active:u.active}}/><div className="page employee-team-page"><ManagerTeamEditor managerId={u.id} selected={u.managedSupervisors.map(x=>x.supervisorId)} supervisors={supervisors.map(x=>({id:x.id,name:x.name,meta:`${x._count.employees} RSOs${x.managerAssignments[0]&&x.managerAssignments[0].managerId!==u.id?` · currently ${x.managerAssignments[0].manager.displayName}`:""}`}))}/></div></>}