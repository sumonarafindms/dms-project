import {requireUser} from "../../../../lib/auth";import {retailerMonthDetail,normalizeMonth} from "../../../../lib/drilldown";import {RetailerDetailView} from "../../../components/DetailViews";import {notFound} from "next/navigation";
export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{month?:string}>}){const u=await requireUser(["SUPERVISOR"]);const {id}=await params,s=await searchParams,month=normalizeMonth(s.month),d=await retailerMonthDetail(id,month);if(!d||d.retailer.employee?.supervisorId!==u.supervisorId)notFound();return <RetailerDetailView d={d} month={month} backHref={d.retailer.employeeId?`/supervisor/rsos/${d.retailer.employeeId}?month=${month}`:"/supervisor/retailers"}/>}
