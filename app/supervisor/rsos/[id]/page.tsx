import {requireUser} from "../../../../lib/auth";import {employeeDetail} from "../../../../lib/employee-detail";import {normalizeMonth} from "../../../../lib/drilldown";import {EmployeeDetailView} from "../../../components/EmployeeDetailView";import {notFound} from "next/navigation";
export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{month?:string;q?:string}>}){const u=await requireUser(["SUPERVISOR"]);const {id}=await params,s=await searchParams,month=normalizeMonth(s.month),d=await employeeDetail(id,month);if(!d||d.employee.supervisorId!==u.supervisorId)notFound();return <EmployeeDetailView d={d} month={month} q={s.q||""} basePath="/supervisor" backHref="/supervisor/rsos"/>}
