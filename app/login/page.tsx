"use client";
import {FormEvent,useState} from "react";
import Link from "next/link";
import {useRouter} from "next/navigation";
export default function Login(){const [admin,setAdmin]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({identifier:f.get("identifier"),credential:f.get("credential"),admin})});const d=await res.json();setBusy(false);if(!res.ok)return setError(d.error||"Sign in failed");router.replace(d.redirect);router.refresh()}return <main className="login-screen"><div className="login-visual"><div className="login-brand"><div className="brand-mark big">D</div><div><h1>DMS</h1><p>Distribution Management System</p></div></div><div className="login-copy"><span>Field sales. Simplified.</span><h2>One platform for your distribution team.</h2><p>Track performance, retailers, sales, targets and field execution from any device.</p></div></div><div className="login-panel"><form className="login-card" onSubmit={submit}><div className="login-mobile-brand"><div className="brand-mark">D</div><strong>DMS</strong></div><div className="login-tabs"><button type="button" className={!admin?"active":""} onClick={()=>{setAdmin(false);setError("")}}>Team Login</button><button type="button" className={admin?"active":""} onClick={()=>{setAdmin(true);setError("")}}>Admin</button></div><h2>{admin?"Admin sign in":"Welcome back"}</h2><p>{admin?"Use your administrator credentials.":"Sign in with your authorized mobile number and PIN."}</p><label className="login-field"><span>{admin?"Username / Mobile":"Mobile Number"}</span><input name="identifier" required placeholder={admin?"admin":"01XXXXXXXXX"}/></label><label className="login-field"><span>{admin?"Password":"PIN"}</span><input name="credential" required type="password" placeholder="••••" inputMode={admin?undefined:"numeric"}/></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary login-btn" disabled={busy}>{busy?"Signing in…":"Sign in"}</button><div className="login-help">Authorized users only. Contact Admin if you cannot access your account.</div><Link href="/setup" className="preview-link">First-time Admin setup →</Link></form></div></main>}
