node_modules/
.next/
.vercel/

# Secrets. `*.env` also covers stray names like notepad.env, which the
# `.env` / `.env.*` patterns do NOT match.
.env
.env.*
*.env
!.env.example

*.log
.DS_Store
node_modules
