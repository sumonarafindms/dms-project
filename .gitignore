node_modules/
.next/
.vercel/

# Secrets. `*.env` also covers stray names like notepad.env, which the
# `.env` / `.env.*` patterns do NOT match.
.env
.env.*
*.env
!.env.example

# TypeScript incremental build cache — a build artifact, not source
*.tsbuildinfo
next-env.d.ts.bak

*.log
.DS_Store
node_modules
